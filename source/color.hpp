#ifndef COLOR_HPP
#define COLOR_HPP
// color class definition
struct Color
{
    Color();
    Color(float);
    Color(float, float, float);
    float r_;
    float g_;
    float b_;
    
};

# endif // COLOR_HPP